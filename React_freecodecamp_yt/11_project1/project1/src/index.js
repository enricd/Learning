/** 
Challenge: Project setup

Import and render teh App component inside of index.js using ReacDOM.
Then go to Google fonts and get the "Inter" font with weights 400, 600, and 700 
above the style.css link in index.html with the <link/> elements. 
 */


import React from "react"
import ReactDOM from "react-dom"
import App from "./App"

ReactDOM.render(<App />, document.getElementById("root"))